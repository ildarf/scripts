#!/bin/bash

if [[ $# -eq 0 || "$1" = "--help" || "$1" = "-h" ]]
then
	echo 'Usage: demo.array3'
	exit 0
fi

get_args()
{
	for i in $*
	do
		if [[ "$1" = "-h" ]]
		then
		echo 'Usage: demo.array3'
			exit 0
		fi
	done
}

get_args $*
